import java.util.List;

public class Visitors3 {
    static public double getDiscount(List<String> visitors) {
	if (visitors.size() > 5) return 0.05;
	else if (visitor.size() > 10) return 0.20;
	else return 0.0;
    }
}
